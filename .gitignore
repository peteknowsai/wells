# Bun / Node
node_modules/
bun.lockb
.bun/

# Build output
dist/
build/

# OS junk
.DS_Store
*.swp
.vscode/
.idea/

# Claude Code session runtime
.claude/scheduled_tasks.lock
.claude/.pete-loop.active
.claude/settings.local.json

# Local sprite/splite project file (per-cwd active splite)
.splite

# Engine source build output — we vendor source, not artifacts
engine/vwell-src/.build/
bin/lume
bin/lume.app/

# Apple signing material — never commit certs/profiles
engine/*.provisionprofile
engine/*.cer
engine/*.p12

# Downloaded base images / disks (large, regenerable)
images/
*.img
*.qcow2
*.iso
*.vmdk

# Logs
*.log

# Env overrides
.env
.env.*
!.env.example
