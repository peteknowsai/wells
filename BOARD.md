# splites — Board

Convention: tasks have IDs `W.{n}` for worker-queue items that don't map to a specific MVP-PLAN checkbox; `phase X.Y.Z` for items that map directly to a checkbox in `docs/MVP-PLAN.md` (close them in MVP-PLAN as part of the same commit). Owner: `worker`, `steward`, or `pete`. Tags: `cells-coordination`, `lume-vendor`, `code`, `docs`, `cost-approval-needed`, `decision-needed`, `needs-pete-session`.

> **State as of 2026-05-10 04:40 UTC:** Cells team unblocked (WS proxy 1011 fix promoted to `wells-stable-2026-05-10a`). 14-item autonomous queue, ~20 hr of runway. **Pete pre-approved shipping without gates** + granted access to `cf` + `wrangler` CLIs (account PKAI, `5a6fef07a998d84ec047ef43d0543342`). Worker can create smoke-only R2 buckets, ship image library, ship lume `@MainActor` fix without checking in.

---

## In Progress

- **W.2 — A.2 R2 round-trip smoke.** Owner: `worker`. **status:** smoke script (`scripts/smoke-r2-sync.ts`, commit `0df2d1c`) shipped — 226 lines, end-to-end create-checkpoint-verify-delete-restore-sha256 flow against dev welld with bucket `wells-smoke-r2`. **Live-verify blocked on W.18** (dev welld first-boot DHCP timeout). Smoke is correct in shape — when W.18 lands, `bun run scripts/smoke-r2-sync.ts` should pass without modification. Don't tick MVP-PLAN A.2 "Smoke: round-trip" until live-verified.

---

## Todo (priority order)

### A.2 R2 polish (Pete's #1)


### Quick wins

### Lume @MainActor variance (Pete's #3) + supporting smokes

- [ ] **W.6 — Lume `@MainActor` variance — diagnose.** Per `docs/MVP-PLAN.md` § B.0.9.d.5.b residual: ~20% of smoke cycles bump 15-15.5s on create+warm because lume's MainActor still occasionally hangs even after B.0.11.h. First fire: instrument `lib/createWell.ts` with per-phase timing logs (clonefile, lume.create, lume.start, waitForDhcpLease #1, waitForSshReady #1, sysrq+disk-release, lume.start #2, waitForDhcpLease #2, waitForSshReady #2). Subsequent fires: run a 50-cycle stress against dev welld :7879, capture distribution to `docs/findings-create-warm-distribution.md`, identify which phase has the long tail. May require lume-side `sample` captures during slow cycles. Owner: `worker`. Tags: `code`, `lume-vendor` (if a fix requires lume changes). **Don't ship a lume patch this fire** — fix scope decided after diagnosis.

### Image library on R2 (Pete's #2 — W.3 → W.4 → W.5)

### Lume variance fix (depends on W.6 findings)

- [ ] **W.7 — Lume `@MainActor` variance — fix or escalate.** Based on W.6 findings: ship a targeted fix (probably async probe machinery instead of bounded blocking) OR write `docs/findings-lume-mainactor-variance.md` if scope is bigger than a fire (e.g., needs lume-side architectural change). Pete pre-approved shipping; no need to escalate the fix approach unless it's truly a multi-day vendor patch. Owner: `worker`. Tags: `code`, `lume-vendor`. **Depends on:** W.6.

### Tech debt + investigations

- [ ] **W.14 — Lume vendor cleanup (only slice 3 left, low value — leave for Pete to call).** Slice 1 + slice 2 shipped 2026-05-10 (commits `831f935`, `ea69e3d`). What's done: `engine/lume.ts` → `engine/vwell.ts`; `vendor/lume/` → `engine/vwell-src/`; entitlements + LICENSE + .txt moved out of vendor; build-lume.sh, .gitignore, all live docs updated; vendor/ removed. **Remaining:** rename `bin/lume` → `bin/vwell`. Low value — `splites-stable/bin/lume` is a wrapper that execs `splites/bin/lume.app/Contents/MacOS/lume`, so renaming forces a stable wrapper update too (+ probably a stable promotion to keep cells team uninterrupted). Defer until Pete asks for it. Owner: `pete`. Tags: `code`, `lume-vendor`.

---

## Blocked

- **W.18 — Dev welld first-boot DHCP timeout.** Full investigation written to `docs/findings-w18-dev-dhcp-timeout.md`. Every `well create --from-image` on dev :7879 times out 90s waiting for the new VM's hostname to land in vmnet's DHCP leases (`lume.info: status=running ip=(none)`). Last successful create on dev was 2026-05-10 03:38 UTC; lume serve hung twice at 03:41 + 03:59 (SIGKILL+respawn — `/tmp/lume-hang-*.txt`). **Stable :7878 unaffected.** Strongest theory: lume-hang aftermath corrupted vmnet bootp state. Tag: `needs-pete-session: try unblock recipe 2 (clean lume+welld restart) and report back`. **Blocks W.2 live-verify + W.6 stress + any dev-side smoke that needs a fresh well.**

---

## Done

_Recently shipped (last ~24h). Older items live in git log + `docs/cells-integration.md` Promotions table._

- [x] **2026-05-10 08:25 UTC** — **W.13 — Concurrent-fork experiment shipped (script).** New `scripts/exp-concurrent-fork.ts` (~360 lines) drives N parallel `well create --from-image` requests for N in {2,3,4,5} (CLI override), captures lume PID + healthz before/after, takes a `sample <pid>` baseline pre-burst, watches `/tmp/lume-hang-*.txt` for new dumps mid-round, classifies fork outcomes (success / 4xx / 5xx / timeout), records lume respawn detection (PID change OR respawns_last_1min increase) and vz_xpc orphan delta. Output: `docs/findings-concurrent-fork-crash.md` per run with summary table + per-round detail tables + "how to read this" interpretation guide. **Live-verify blocked on W.18.** When dev unblocks this is the diagnostic that nails B.0.11.d's threshold (where does lume start respawning under fan-out load?). Pairs with W.10 (wake stress) + W.11 (pool churn) for the full lume-stress picture cells team needs for scale planning. 520/520 still green.
- [x] **2026-05-10 08:10 UTC** — **W.11 — Pool churn smoke shipped.** New `scripts/smoke-pool-churn.ts` (~270 lines) sets `defaults.pool_size=2` (with restore-on-exit guard), waits for initial fill, then runs 20 back-to-back `create → destroy` cycles asserting pool depth stays maintained. After sequential churn, kicks off 3 concurrent creates as a fan-out burst — asserts (a) every successful create's `lume_name` is distinct (no double-adoption race in `reserveReadyMember`), (b) pool refills back to target after the fan-out destroys complete. Reads `/healthz` `pool` block (W.9) for depth sampling. Cleanup is `try/finally`-guarded so a failure mid-run still restores defaults + destroys lingering wells + drains pool. **Live-verify blocked on W.18.** Build clean, 520/520 still green. Pairs with W.10 — when dev unblocks, both smokes feed the wake/pool variance picture for cells team's "scale fan-out" planning.
- [x] **2026-05-10 08:00 UTC** — **W.10 — Wake reliability stress smoke shipped.** New `scripts/smoke-wake-stress.ts` (~330 lines) reuses one well across 30 hibernate→wake cycles, captures per-cycle hibernate/wake/ssh-after-wake timings, computes min/p50/p95/p99/max, and writes a self-contained `docs/findings-wake-stress-<date>.md` per run with verdict + raw data. Gates: p95 wake ≤2000ms, p95 ssh-after-wake ≤2500ms (CLI-overridable). Reuses existing well if present (`--keep` from prior run); falls back to fresh-create. Will surface lume `@MainActor` variance more cleanly than the create+warm path because hibernate↔wake isolates the lume info()/restoreState path under load. **Live-verify blocked on W.18** (dev welld DHCP — needed for the well-create fallback path). When W.18 unblocks, `bun run scripts/smoke-wake-stress.ts` should produce the first real distribution data we'll have for wake variance — feeds straight into W.6/W.7. 520/520 still green.
- [x] **2026-05-10 07:50 UTC** — **W.14 slice 2 — `vendor/` is gone.** Moved `vendor/lume/` → `engine/vwell-src/`, `vendor/lume.txt` → `engine/vwell-src.txt` (rewritten to capture the in-tree edits history), entitlements file → `engine/well-engine.entitlements`, LICENSE → `engine/vwell-src.LICENSE.md`. Also moved the gitignored `splites-lume.provisionprofile` to `engine/`. Deleted `vendor/lume.patches/README.md` (history in git log) and removed the empty `vendor/` dir. Updated `scripts/build-lume.sh` (LUME_SRC + ENTITLEMENTS), `.gitignore` (build output + signing material paths), and 8 docs (CLAUDE.md, BLOCKED, MVP-PLAN, memory-budget, state-tiers, findings-lume-orphan-sweep, decisions/0001-engine-lume, proposals/B.0.6). LumeClient class + `bin/lume` binary unchanged. 520/520 green. Build script not re-run this fire (rebuilding bin/lume.app needs Swift + signing material; production binary on disk works fine). Commit `ea69e3d`. **Only `bin/lume` rename left** — low value, deferred to Pete.
- [x] **2026-05-10 07:35 UTC** — **W.14 slice 1 — `engine/lume.ts` → `engine/vwell.ts`.** Pure file-path rename: 8 code files (`lib/*`, `daemon/welld.ts`, `scripts/bake-base-image.ts`) + the test file (renamed too) + 9 docs (CLAUDE.md, architecture, BLOCKED, MVP-PLAN, ROADMAP, findings-fork-from-saved, memory-budget, state-tiers, cells-integration) updated. `LumeClient` class name and the lume binary at `bin/lume` are unchanged. `vendor/lume/` source tree untouched. 520/520 green; welld + cli build clean. Commit `831f935`. Slices 2+ (vendor source tree, entitlements move, build script, optional bin rename) stay queued; collision note logged in BOARD.
- [x] **2026-05-10 07:25 UTC** — **W.19 + W.20 — both audit follow-ups shipped.** W.19: `uncaughtException` handler at `daemon/welld.ts:150` now detects port-bind failures (`Failed to start server` / `EADDRINUSE`) and `process.exit(1)` instead of swallowing — kills the zombie-welld pattern from the audit. W.20: watchdog `stopWell` callback in welld.ts now tracks per-well consecutive hibernate failures; after 5 consecutive fails it logs `warn` once and skips future attempts (counter resets on success, or on welld restart). Closes the "stuck well retried every tick forever" log noise. 520/520 green; both fixes are observable behavior changes (no unit test value — process.exit and Map mutation are inert outside the runtime).
- [x] **2026-05-10 07:15 UTC** — **W.12 — welld log audit done.** Walked stable + dev welld.log, classified all `level: error` and `level: warn` entries over 2 days. Four patterns: (A) `uncaught exception — continuing` for port-bind failures, 7 hits — should exit, not swallow → **W.19** added. (B) `watchdog: hibernate failed` per-well loop, no backoff → **W.20** added. (C) `lume serve unresponsive; respawning` 60x — already tracked as W.6/W.13/B.0.11.h. (D) supervisor warns 1-6x — informational. No new ssh/R2/cell-sleep error patterns. Findings in `docs/findings-welld-rejection-audit-2026-05-10.md`. Per W.12's contract worker did NOT ship the W.19/W.20 fixes this fire.
- [x] **2026-05-10 07:05 UTC** — **W.15 — Test isolation flakies, investigated.** Three back-to-back default-mode runs ship 520/520 green; iteration-4's 13-fail pattern doesn't reproduce. Under `bun test --concurrent`, however, 85/520 tests fail because 13 lib/*.test.ts files share `process.env.WELL_STATE_DIR` across tests (`beforeEach` overwrites the env, concurrent tests trample). That's a known constraint of the current test layout, not a bug — Bun's default sequential mode is the safety net. Findings + recommendation in `docs/findings-w15-test-isolation.md`: don't use `--concurrent` (and if we ever need to, refactor the 13 files to inline `mkdtemp` per test). Added `npm test` script to package.json so `bun test` invocation stays uniform. Added a header comment to `checkpoints.test.ts` warning future readers about the env-mutation pattern. Iteration-4's specific 13-fail run is most likely **system-load-induced timing** during heavy welld/lume churn (run took 69s vs 700ms baseline) — flagged in the doc but not actionable without a reproducer.
- [x] **2026-05-10 06:55 UTC** — **W.5 — Image library pull half + auto-pull on create.** `pullImage(name, config, deps?)` in `lib/imageLibrary.ts`: fetch manifest → stream disk to `disk.img.partial` → sha256 verify against manifest → size verify → fetch meta → rotate disk into place → write meta. Sha256 mismatch deletes the partial and refuses to overwrite local — caller's old copy survives. Welld `POST /v1/wells/images/<name>/pull` with shared `resolveR2LibraryConfig` (body or `WELL_R2_LIBRARY_*` env). CLI `well image pull <name>` with `--force` to re-pull when local exists. **Auto-pull on `well create --from-image`**: when the image is missing locally AND `WELL_R2_LIBRARY_*` env is set, `createWell` pulls from R2 before throwing not-found. Default base image excluded (bake script is canonical for it). 6 new pull tests + 1 push-pull round-trip integration test. 520/520 green.
- [x] **2026-05-10 06:42 UTC** — **W.4 — Image library push half.** New `lib/imageLibrary.ts` with `pushImage(name, config, welldVersion, deps?)` — streams `disk.img` from local `~/.wells/images/<name>/` through a Bun.CryptoHasher (sha256), then via `S3Client.write` to `images/<name>/{disk.img, meta.json, manifest.json}` in that order (manifest last so a partial upload doesn't expose an apparently-complete image). Manifest carries sha256 + size + pushed_at + welld version + host. Welld endpoint `POST /v1/wells/images/<name>/push` reads R2 config from request body OR `WELL_R2_LIBRARY_*` env (per-Mac default with per-image override path). CLI `well image push <name>`. 7 new unit tests with stub S3Client (key order, manifest shape, meta verbatim copy, missing-local error, malformed-local error). 514/514 green. W.5 (pull half) implements next.
- [x] **2026-05-10 06:32 UTC** — **W.3 — Image library on R2: design shipped.** `docs/proposals/image-library-on-r2.md` (~280 lines) covers bucket layout (`<bucket>/images/<name>/{manifest.json, meta.json, disk.img}` — manifest carries R2-only fields like sha256/pushed_at; meta.json is verbatim copy of local meta so a fresh-pulled image is byte-equivalent to a locally-baked one), name-as-version (no tags in v1), per-Mac creds via `WELL_R2_LIBRARY_*` env + per-image override, CLI surface (`push/pull/list --remote/rm --remote` + auto-pull on `well create --from-image`), security boundary (read = "can boot any image"), Phase E fit (image library is a Colony prerequisite; this design lands the primitive). W.4 and W.5 implement against this spec.
- [x] **2026-05-10 06:25 UTC** — **W.18 partial — investigated to a clear blocker.** Walked the welld log, dhcpd_leases (240 entries — large but stable still works), cidata.iso (correct shape), zombie VMs (stopped my orphan `r2smokezd70vt`; left `rawtest` alone — unknown owner). Identified the timeline: last successful dev create 03:38 UTC → lume hung at 03:41 + 03:59 (sample dumps in `/tmp/lume-hang-*`) → every fresh-create since fails the same way. Cidata is correct, vmnet has IPs available, welld restart didn't fix it — issue is below welld in the lume/vmnet layer. Findings written to `docs/findings-w18-dev-dhcp-timeout.md` with 4 unblock recipes (least → most invasive). Moved W.18 to **Blocked** pending Pete's lume+welld clean restart. Worker didn't bounce vmnet itself (sudo, infra) or stop `rawtest` (not ours).
- [x] **2026-05-10 06:18 UTC** — **W.8 — MVP-PLAN § A.1.3 audit.** Ticked three sub-boxes that shipped via the B.0.9 / A.1.4 workstreams but never got marked done: A.1.3.e.2 (hibernation lume patch — `engine/lume.ts:saveState/restoreState` + production hibernate.bin round-trip), A.1.3.f (wire tiers into welld — all sub-boxes done), A.1.3.f.3b (watchdog hibernate path — `daemon/welld.ts:1629-1670` calls `transitionWell(name, "hibernate", …)` per Pete's "sleep means hibernate" contract). Left A.1.3.c (full benchmark p50/p95) and A.1.3.g (scenario-coverage smoke for all 10 from A.1.3.a) unticked — those need real work, not just doc reconciliation. A.1.3 parent stays unchecked until c and g land.
- [x] **2026-05-10 06:13 UTC** — **W.17 — `well exec --user=<user>` equals-syntax (cells team).** Investigation found the `--user` flag already existed (`cli/well.ts:498`, defaults to `well`). The actual gap was in `lib/parseExecArgs.ts` — it only accepted space-separated values (`--user cell`), not equals-syntax (`--user=cell`). Cells team automation's `--user=cell` was failing with `unknown flag '--user=cell'`. Fix: parser now splits on first `=` for any `--flag=value` or `-f=value` shape across `--well`, `--user`, `-s`, `-u`. `--tty=anything` rejects with "takes no value" (boolean flag). 7 new tests including IAM-style `=`-in-username edge case. Default user stays `well` (cells team will get user `cell` once they finish their /home/-to-/cell/ migration; we don't shift the default until they ask). 507/507 green.
- [x] **2026-05-10 06:08 UTC** — **W.9 — `/healthz` exposes pool depth.** New `pool` block on `/healthz`: `{target_size, ready_count, provisioning_count, warming_count, adopting_count}`. New `poolSummary(targetSize)` helper in `lib/poolRegistry.ts` walks the registry once and counts by state. target_size sourced from `defaults.pool_size` so cells team monitoring sees both the configured intent AND the current readiness. 3 new unit tests + live-verified on dev welld :7879 — pool block lands in JSON response. 500/500 green.
- [x] **2026-05-10 06:10 UTC** — **W.16 — fork-empty-home rinse claim resolved by cells team.** Cells team responded: their fix, not ours. They're moving DNA out of `/home/well/` to `/cell/` (`~2h grep-and-replace through birth skill + bake script`). Wells's rinse stays as-is (already identity-only per `lib/rinseWell.ts:47-58`). Their only ask: doc note explaining exactly what `validate=true` rinses. Worker added that note to `docs/cells-integration.md` (canonical list of paths + recommendation to put cell content outside `/home/`). NEEDS_PETE.md kept on disk as audit trail; closing W.16 here since coordination is resolved.
- [x] **2026-05-10 05:45 UTC** — **W.1 — A.2 R2 GC tracks local retention.** Implementation was already in place (`lib/r2.ts:83` env-guard + `lib/checkpoints.ts:dropCheckpoint` calls r2Delete when checkpoint had `r2_uploaded=true` and well has R2 config). What was missing: tests. Added 3: rotate-with-r2 (verifies r2Delete called for evicted checkpoints with r2_uploaded=true), rotate-without-r2 (verifies r2Delete never called when well has no R2 config), and the env opt-out (verifies `WELL_R2_RETAIN_FOREVER=1` short-circuits before any S3 call, asserted via <50ms wall-clock against an unreachable endpoint). 497/497 green. Closed MVP-PLAN § A.2 box. Commits: this fire.
- [x] **2026-05-10 05:40 UTC** — Stable promoted to `wells-stable-2026-05-10b` (commit `af21853`) so the supervisor fix is in place for cells team. Welld restarted; healthz reports `lume.owned: true` (spawned, supervised). Stable is back up.
- [x] **2026-05-10 05:30 UTC** — Lume supervisor adopted-gap fix shipped to dev. `engine/lumeProcess.ts` now ALWAYS supervises whatever lume is on `WELL_LUME_PORT` (spawn or adopt), with lsof-based PID lookup for fast-exit detection on adopted lumes. Closes the silent supervisor gap that bit cells team at 04:29-05:11 UTC (stable welld adopted lume on startup → didn't supervise → lume hung + died → no respawn → `status: missing` across every well). Live-verified on dev: kill adopted lume → respawn within one tick (~5s). `WELL_LUME_NO_SUPERVISE=1` opt-out for debugging. Commit: `b27ad05`. **Stable promotion is Pete's call.**
- [x] **2026-05-10 04:22 UTC** — WS proxy 1011 fix shipped + promoted to `wells-stable-2026-05-10a`. `lib/proxy.ts:buildUpstreamWsInit` forwards client headers + subprotocols to upstream WS. Cells team `cells talk` repro unblocked. Commits: `9c7a34c`, `59b2941`, `3477980`, `41d92ab`. See `docs/cells-integration.md` for the full Promotions row.
- [x] **2026-05-09** — A.1 phase fully shipped (pre-warmed pool, sub-3s `well create`, `well pool` CLI + REST). Promoted to `wells-stable-2026-05-09j`.
